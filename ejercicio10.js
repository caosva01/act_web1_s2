let usuario = "admin";
let contrasena = "1234";

// Comprobamos si ambos son correctos al mismo tiempo
if (usuario === "admin" && contrasena === "1234") {
  console.log("acceso concedido");
} else {
  console.log("acceso denegado");
}
